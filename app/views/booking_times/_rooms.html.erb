<% @rooms.in_groups_of(2) do |slice| %>
  <div class="row">
    <% slice.each do |r| %>
      <% next if r.nil? %>
      <div class="col-sm-6">
        <%= render 'room', f: r %>
      </div>
    <% end %>
  </div>
<% end %>