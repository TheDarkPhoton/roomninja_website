<%= @time_begin %><br>
<%= @time_end %><br>
<%= @date_begin %><br>

<% @rooms.in_groups_of(3) do |slice| %>
  <div class="row">
    <% slice.each do |r| %>
      <% next if r.nil? %>
      <%= render 'room', f: r %>
    <% end %>
  </div>
<% end %>